# Plan: v1.3 Data — 4 новых скилла (sql-helper, csv-pro, web-scraper, data-analysis)

> Дата: 2026-08-08
> Статус: `approved`

## Goal
Выпустить этап v1.3: четыре скилла (`sql-helper`, `csv-pro`, `web-scraper`,
`data-analysis`) в каталоге `index.json` (21 скилл), пройти валидатор,
закоммитить, запнуть и установить локально в `~/.config/opencode/skills`.

**Acceptance criteria (проверяемо):**
- [ ] CR1: `python3 .github/workflows/validate_skills.py` → 21/21 OK
- [ ] CR2: каждый скилл имеет `SKILL.md` + `skill.json` + рабочий скрипт в `scripts/`
- [ ] CR3: smoke-запуск всех четырёх скриптов на тест-данных без ошибок
- [ ] CR4: ROADMAP v1.3 все пункты `[x]`, CHANGELOG добавлен, README/README.ru обновлены

## Constraints
- Python 3 stdlib для ядра; опциональные внешние тулзы через subprocess (скрейпер)
- Не трогать чужие скиллы и существующий `index.json` кроме добавления 4 записей
- Категории из существующего набора: repository, media, social, code, data
- Триггеры и описания в `skill.json` согласовать с `SKILL.md` (нет дрейфа)

## Steps

### Step 1: sql-helper скрипт
- Files: `skills/sql-helper/scripts/sql_helper.py`
- Produces: `generate(ddl, question)` → SQL, `explain(sql)` → разбор плана
- Consumes: `sqlite3`, `argparse` — stdlib
- Action: генерация по словарю шаблонов, EXPLAIN через sqlite3, форматирование
- Verification: `python3 sql_helper.py --ddl sample.sql --question "join users"` → SQL без ошибок
- [ ] done

### Step 2: sql-helper манифест
- Files: `skills/sql-helper/SKILL.md`, `skills/sql-helper/skill.json`
- Produces: скилл регистрируется в каталоге (category `data`)
- Action: frontmatter, разделы When to use/How to work, триггеры "sql"/"запрос"
- Verification: `validate_skills.py` включает скилл без ошибок
- [ ] done

### Step 3: csv-pro скрипт
- Files: `skills/csv-pro/scripts/csv_pro.py`
- Produces: `profile(csv_path)` → отчёт: тип/мин/макс/средн/пропуски, аномалии
- Consumes: `csv`, `statistics`, `collections` — stdlib
- Action: чтение CSV, вывод профиля полей, поиск аномалий (пусто/докер)
- Verification: smoke на `data/sample.csv` → профиль и список аномалий
- [ ] done

### Step 4: csv-pro манифест
- Files: `skills/csv-pro/SKILL.md`, `skills/csv-pro/skill.json`
- Produces: скилл валиден (category `data`)
- Action: профилирование/аномалии/визуализация через вывод таблиц markdown
- Verification: `validate_skills.py` без ошибок
- [ ] done

### Step 5: web-scraper скрипт
- Files: `skills/web-scraper/scripts/scrape.py`
- Produces: `scrape(url, selectors)` → данные в JSON/markdown
- Consumes: `urllib.request`, `html.parser` — stdlib, robots.txt проверка
- Action: `--url/--selector/--output`, задержка между запросами, guardrails
- Verification: запуск на локальном file:// тесте → структура данных без ошибок
- [ ] done

### Step 6: web-scraper манифест
- Files: `skills/web-scraper/SKILL.md`, `skills/web-scraper/skill.json`
- Produces: скилл валиден (category `data`)
- Action: легальные guardrails (robots.txt, rate limit, ссылки на ToS)
- Verification: `validate_skills.py` без ошибок
- [ ] done

### Step 7: data-analysis скрипт
- Files: `skills/data-analysis/scripts/data_analyze.py`
- Produces: `analyze(dataset_path)` → профиль, корреляции, рекомендации
- Consumes: `json`, `csv`, `statistics` — stdlib
- Action: принимает CSV/JSON, выводит статистику, аномалии, текст-итог
- Verification: smoke на тестовом dataset → отчёт с секциями без ошибок
- [ ] done

### Step 8: data-analysis манифест
- Files: `skills/data-analysis/SKILL.md`, `skills/data-analysis/skill.json`
- Produces: скилл валиден (category `data`)
- Action: распределение профилей, секции отчёта, триггеры "analysis"/"данные"
- Verification: `validate_skills.py` без ошибок
- [ ] done

### Step 9: каталог и документация
- Files: `index.json`, `ROADMAP.md`, `CHANGELOG.md`, `README.md`, `README.ru.md`
- Produces: 21 запись в каталоге, v1.3 полностью `[x]`, changelog и README обновлены
- Action: добавить 4 записи сортированно, поменять статусы, синхронизировать RU-зеркало
- Verification: `python3 .github/workflows/validate_skills.py` → 21/21 OK
- [ ] done

### Step 10: commit + push + установка
- Files: git-коммит на main, пуш, `rsync skills/* -> ~/.config/opencode/skills/`
- Produces: коммит-запись, удалённый main обновлён, 21 скилл локально
- Action: два коммита (docs + skills), push, rsync четырёх новых папок локально
- Verification: `git status` чистый, CI-статус Validate Skills success
- [ ] done

## Interfaces
- Consumes → Produces:
  - `Step1.sql_gen` → `Step2.meta_sql`
  - `Step3.csv_profile` → `Step4.meta_csv`
  - `Step5.scrape` → `Step6.meta_scraper`
  - `Step7.analyze` → `Step8.meta_data`
  - `Step2/4/6/8.meta` → `Step9.catalog`
  - `Step9.catalog` → `Step10.release`

## Verification (полная)
- [ ] `python3 skills/plan-skill/scripts/plan_validator.py docs/plans/v1.3-plan.md` → ✅
- [ ] `python3 .github/workflows/validate_skills.py` → 21/21 OK
- [ ] Smoke: 4 скрипта на тестовых данных — без ошибок
- [ ] Коммиты + push сделаны, CI зелёный, локальная установка 21 скилла