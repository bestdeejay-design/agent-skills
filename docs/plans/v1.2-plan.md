# Plan: v1.2 Visual & Docs — 3 новых скилла (mermaid-to-image, pdf-report-builder, video-script-writer)

> Дата: 2026-08-08
> Статус: `approved`

## Goal
Выпустить этап v1.2: три скилла (`mermaid-to-image`, `pdf-report-builder`,
`video-script-writer`) в каталоге `index.json` (17 скиллов), пройти валидатор,
закоммитить, запнуть и установить локально в `~/.config/opencode/skills`.

**Acceptance criteria (проверяемо):**
- [x] CR1: `python3 .github/workflows/validate_skills.py` → 17/17 OK
- [x] CR2: каждый скилл имеет `SKILL.md` + `skill.json` + рабочий скрипт в `scripts/`
- [x] CR3: smoke-запуск всех трёх скриптов на тест-данных без ошибок
- [x] CR4: ROADMAP v1.2 все пункты `[x]`, CHANGELOG добавлен, README/README.ru обновлены

## Constraints
- Python 3 stdlib + опциональные внешние тулзы (mmdc/pandoc) через `subprocess`
- Не трогать чужие скиллы и существующий `index.json` кроме добавления 3 записей
- Категории из существующего набора: repository, media, social, code, data
- Триггеры и описания в `skill.json` согласовать с `SKILL.md` (нет дрейфа)

## Steps

### Step 1: mermaid-to-image скрипт
- Files: `skills/mermaid-to-image/scripts/mermaid_to_image.py`
- Produces: `render_mermaid(mmd_text, out, fmt)` → PNG/SVG через mmdc, fallback mermaid.ink
- Consumes: `subprocess`, `urllib` — stdlib
- Action: парсер `--input/--output/--format/--background`, mmdc предпочтителен, mermaid.ink фолбэк
- Verification: `python3 mermaid_to_image.py --input sample.mmd --output out.svg` → файл создан
- [x] done

### Step 2: mermaid-to-image манифест
- Files: `skills/mermaid-to-image/SKILL.md`, `skills/mermaid-to-image/skill.json`
- Produces: скилл регистрируется в каталоге (метаданные валидны)
- Action: frontmatter, разделы When to use/How to work, category `data`, триггеры `.mmd`
- Verification: `validate_skills.py` включает скилл без ошибок
- [ ] done

### Step 3: pdf-report-builder скрипт
- Files: `skills/pdf-report-builder/scripts/pdf_report_builder.py`
- Produces: `build(md_text, out, title)` → PDF через pandoc/weasyprint — первый доступный
- Consumes: `subprocess`, детект тулов по `shutil.which`
- Action: `--input/--output/--title`, md→HTML→PDF, вывод списка тул if недоступен
- Verification: smoke-запуск на `README.md` тестовый → PDF созданён (или ясное сообщение)
- [ ] done

### Step 4: pdf-report-builder манифест
- Files: `skills/pdf-report-builder/SKILL.md`, `skills/pdf-report-builder/skill.json`
- Produces: скилл валиден (category `media`)
- Action: описание генерации отчётов, шаблоны линков на тулзы, триггеры "отчёт"/"report pdf"
- Verification: `validate_skills.py` без ошибок
- [ ] done

### Step 5: video-script-writer скрипт
- Files: `skills/video-script-writer/scripts/video_script_writer.py`
- Produces: `topic → outline(duration)`, полный сценарий hook/body/CTA
- Action: `--topic/--duration/--lang/--cta`, генерация markdown-сценария по разбивке на секунды
- Verification: запуск с тестом топики → файл сценария с секциями Hook/Body/CTA
- [ ] done

### Step 6: video-script-writer манифест
- Files: `skills/video-script-writer/SKILL.md`, `skills/video-script-writer/skill.json`
- Produces: скилл валиден (category `media`)
- Action: структура сценария, секции timing, триггеры "video script"/"сценарий видео"
- Verification: `validate_skills.py` без ошибок
- [ ] done

### Step 7: каталог и документация
- Files: `index.json`, `ROADMAP.md`, `CHANGELOG.md`, `README.md`, `README.ru.md`
- Produces: 17 записей в каталоге, v1.2 полностью `[x]`, changelog и README обновлены
- Action: добавить 3 записи сортированно, поменять статусы, синхронизировать RU-зеркало
- Verification: `python3 .github/workflows/validate_skills.py` → 17/17 OK
- [ ] done

### Step 8: commit + push + установка
- Files: git-коммит на main, пуш, `rsync skills/* -> ~/.config/opencode/skills/`
- Produces: коммит-запись, удалённый main обновлён, 17 скиллов локально
- Action: два коммита (docs + skills), push, rsync трёх новых папок локально
- Verification: `git status` чистый, CI-статус Validate Skills success
- [ ] done

## Interfaces
- Consumes → Produces:
  - `Step1.render_mmd` → `Step2.meta_mermaid`
  - `Step3.md_to_pdf` → `Step4.meta_pdf`
  - `Step5.script_gen` → `Step6.meta_video`
  - `Step2/4/6.meta` → `Step7.catalog`
  - `Step7.catalog` → `Step8.release`

## Verification (полная)
- [x] `python3 skills/plan-skill/scripts/plan_validator.py docs/plans/v1.2-plan.md` → ✅
- [x] `python3 .github/workflows/validate_skills.py` → 17/17 OK
- [x] Smoke: 3 скрипта на тестовых данных — без ошибок
- [x] Коммиты + push сделаны, CI зелёный, локальная установка 17 скиллов