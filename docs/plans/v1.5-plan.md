# Plan: v1.5 Automation & Release — 4 новых скилла (version-bumper, commit-lint, coverage-analyzer, api-contract-testing)

> Дата: 2026-08-09
> Статус: `approved`
> Bootstrap: pipeline plan_Validator v1.5 (замыкает цикл кода v1.1)

## Goal

Выпустить этап v1.5: четыре кодовых скилла (`version-bumper`, `commit-lint`,
`coverage-analyzer`, `api-contract-testing`) в каталоге `index.json` (28 скиллов),
пройти валидатор, закоммитить, запушить и установить локально в
`~/.config/opencode/skills`.

**Acceptance criteria (проверяемо):**
- [ ] CR1: `python3 .github/workflows/validate_skills.py` → 28/28 OK
- [ ] CR2: каждый скилл имеет `SKILL.md` + `skill.json` + скрипт в `scripts/` + `references/canonical-patterns.md`
- [ ] CR3: четыре скрипта проходят smoke-запуск на реальных входных данных репозитория без ошибок
- [ ] CR4: ROADMAP v1.5 все пункты `[x]`, CHANGELOG добавлен, README/README.ru обновлены

## Constraints

- Python 3 stdlib для ядра каждого скрипта; внешние тулзы через subprocess (osv)
- Категория всех четырёх скиллов — `code`
- Не трогать существующие скиллы и записи `index.json` кроме добавления 4 новых
- Триггеры и описания в `skill.json` синхронизировать с `SKILL.md` (нет дрейфа)
- Showcase-примеры на проекте `lovii_demo` (`/Users/best/Projects/lovii_demo`)

## Steps

### Step 1: version-bumper скрипт
- Files: `skills/version-bumper/scripts/bumper.py`
- Produces: bump след. версии по Conventional Commits + предложение тега релиза
- Consumes: stdlib `re`, `subprocess` (git), `argparse`
- Action: парсинг git log, классификация feat/fix/breaking → bump (major/minor/patch)
- Verification: на `skills-repo` git log → выводит следующий тег (v1.5.0)
- [ ] done

### Step 2: version-bumper манифест
- Files: `skills/version-bumper/SKILL.md`, `skills/version-bumper/skill.json`
- Produces: скилл регистрируется в каталоге (category `code`, triggers EN/RU)
- Action: frontmatter, разделы When/How, ссылка на canonical-patterns
- Verification: `validate_skills.py` включает скилл без ошибок
- [ ] done

### Step 3: commit-lint скрипт
- Files: `skills/commit-lint/scripts/commit_lint.py`
- Produces: `lint(commit_msg)` → pass/fail по Conventional Commits правилам
- Consumes: stdlib (sys, re, argparse); задаётся `--header`
- Action: валидация формата (type(scope)!: subject, длина, стоп-слова body)
- Verification: smoke на корректном и некорректном заголовке → exit 0/1
- [ ] done

### Step 4: commit-lint манифест
- Files: `skills/commit-lint/SKILL.md`, `skills/commit-lint/skill.json`
- Action: регистрация в каталоге, triggers ("commit lint", "conventional commits")
- Verification: `validate_skills.py` без ошибок
- [ ] done

### Step 5: coverage-analyzer скрипт
- Files: `skills/coverage-analyzer/scripts/coverage_analyzer.py`
- Consumes: coverage.py XML (через `-` stdin/файл) — stdlib `xml.etree`
- Produces: отчёт: total%, top-lowest files, дельта vs baseline
- Verification: парс тестового coverage.xml → стабильный отчёт (total_pct)
- [ ] done

### Step 6: coverage-analyzer манифест
- Files: `skills/coverage-analyzer/SKILL.md`, `skills/coverage-analyzer/skill.json`
- Action: регистрация, triggers "coverage", "покрытие", category code
- Verification: валидатор green
- [ ] done

### Step 7: api-contract-testing скрипт
- Files: `skills/api-contract-testing/scripts/api_contract.py`
- Consumes: OpenAPI JSON (stdin/--file), stdlib `json`
- Produces: манифест проверок: endpoints, required fields, response codes
- Verification: smoke на `openapi.json` (заготовка) → N endpoints, 0 ошибок
- [ ] done

### Step 8: api-contract-testing манифест
- Files: `skills/api-contract-testing/SKILL.md`, `skills/api-contract-testing/skill.json`
- Action: регистрация, triggers "openapi", "api contract", "контракт API"
- Verification: валидатор green
- [ ] done

### Step 9: интеграция + валидация
- Files: `index.json`, `README.md`, `README.ru.md`, `ROADMAP.md`, `CHANGELOG.md`
- Action: 4 записи в `index.json`, таблицы/дерево в README (EN+RU), v1.5 [x],
  CHANGELOG [Unreleased] → Fixed/Added; запуск `validate_skills.py`
- Verification: CR1–CR4 (28/28, smoke, документация) — зелёный статус
- [ ] done

### Step 10: синхронизация локальной установки
- Files: `~/.config/opencode/skills/*` (version-bumper, commit-lint,
  coverage-analyzer, api-contract-testing)
- Action: rsync 4 папок из `skills-repo/skills/` в `~/.config/opencode/skills/`
- Verification: `find ~/.config/opencode/skills -maxdepth 1 -type d` содержит все 4
- [ ] done

## Verification

- CR1: `validate_skills.py` exit 0, 28/28
- CR2: ls каждой папки скилла содержит SKILL.md, skill.json, scripts/*.py, canonical-patterns.md
- CR3: ручной smoke: `bumper.py` (git log), `commit_lint.py` (2 записи),
  `coverage_analyzer.py` (coverage sample), `api_contract.py` (openapi sample)
- CR4: grep ROADMAP v1.5 ✓, CHANGELOG [Unreleased] Added-секция ✓, README таблицы
- Финальный майер: commit `feat(v1.5)` + tag `v1.5.0` + push (по отдельному запросу)