# Showcase: `<skill-name>` on a real project

> Демонстрация работы скилла на **реальном** проекте (не абстрактном `foo.txt`).
> Эталонный проект: **lovii.ru** — https://github.com/bestdeejay-design/lovii_demo
> (`index.html`, `index.js`, `css/`, `js/`, `design/`, `docs/`).
> Стиль вдохновлён разделами «Examples» в Anthropic skills.

## 1. Вход (Input)

| Что | Где |
|---|---|
| Проект (репозиторий) | `https://github.com/bestdeejay-design/lovii_demo` |
| Файлы, к которым применяется скилл | `<относительный путь, например: docs/PRD_DEMO.md>` |
| Задача для скилла | Что конкретно скилл должен сделать с этими файлами |

*Почему именно эти данные:* короткое обоснование выбора (реальность, типичность,
наличие нетривиальных кейсов — экраны, JSON-вставки, специфичный HTML и т.п.).

## 2. Запуск (Run)

```bash
# точная команда (с реальными путями к проекту)
python3 skills/<skill-name>/scripts/<script>.py --input <project-path>/<file>
```

## 3. Вывод (Output)

Сокращённый **реальный** вывод (первые N строк) или путь к артефакту:

```text
…сокращённый реальный вывод…
```

Полный вывод/артефакт: `docs/showcase/<skill-name>-output.<ext>` (опционально).

## 4. Интерпретация (Interpretation)

- Что результат означает (какие аномалии/свойства/данные вскрылись).
- Чем полезно владельцу `lovii_demo` (на что обратить внимание).
- Ограничения применения скилла к этому конкретному проекту (если есть).

---

> Чек-лист перед принятием showcase:
> - [ ] входные пути ведут в реальный репозиторий (не абстрактные примеры);
> - [ ] команда запуска воспроизводима (`bash -c` командой из шага 2);
> - [ ] вывод в шаге 3 — реальный (не придуманный).